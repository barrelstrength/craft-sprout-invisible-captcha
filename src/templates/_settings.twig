{% import "_includes/forms" as forms %}

{{ forms.lightswitchField({
    label: 'Enable Javascript method',
    instructions: 'Prevent a form from being submmitted if a user does not have JavaScript enabled ',
    name: 'captchaSettings[googleRecaptchaSiteKey]',
    //TODO add settings under integrationId
    toggle: captcha.getCaptchaId(),
    on: isEnabled,
    onLabel: "Enable"|t,
}) }}

{{ forms.textField({
    label: '',

    id: 'googleRecaptchaSiteKey',
    name: 'captchaSettings[googleRecaptchaSiteKey]',
    required: true,
    errors: null,
    value: settings['googleRecaptchaSiteKey'] ?? null
}) }}

{{ forms.textField({
    label: 'Secret key',
    instructions: 'Get the secret key on your dashboard',
    id: 'googleRecaptchaSecretKey',
    name: 'captchaSettings[googleRecaptchaSecretKey]',
    required: true,
    value: settings['googleRecaptchaSecretKey'] ?? null
}) }}